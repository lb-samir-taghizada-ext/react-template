const initialState = {
  isLoggedIn: true,
  email: '',
  name: '',
}

export const userReducer = (state = initialState, action) => {
  // returns the state
  // switch () {

  // }
  return state;
}

const USER_ACTION_TYPES = {

}